-----El estado como una instantánea-----
las variables se comportan mas como instantánea que como variables normales de Javascript
* DEsencadena estados: 
* Instantánea en el tiempo: 
* Cuando coloco llaves en el jsx <button{}> Dentro va el código Javascript y quiere decir que se ejecuta inmediatamente esoq uiere decir que se debe correr esa parte del código cuando se de click (ocurra el evento) y no antes
----Poner en cola ----
batching: la práctica de agrupar múltiples actualizaciones de estado en un solo ciclo de renderizado en lugar de realizar renderizados independientes para cada actualización de estado.