/*import React, {useState} from "react";

function VampireToggle(){
    const [isVampire, setIsVampire] = useState(false);

    return(
        <div>
            <p>Eres {isVampire ? 'un vampiro' : 'un humano'}</p>
            <button onClick={() => setIsVampire(!isVampire)}>Cambiar forma</button>
        </div>
    );
};*/